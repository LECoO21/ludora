# Ludora UI 与首页简化开发记录

> 本文记录实际已经完成的开发、验证结果和待人工确认项。计划与技术方案分别见《Ludora-UI与首页简化技术排期手册》和《Ludora-UI与首页简化技术开发手册》。

## 1. 记录信息

| 项目 | 内容 |
| --- | --- |
| 记录日期 | 2026-09-04；最近更新 2026-09-05 |
| 本轮主题 | Ludora 首页与游戏制作工作台简化 |
| 当前状态 | 代码实现完成，自动化验证通过，等待最终人工体验确认 |
| 视觉方向 | 中性灰工作台；主题色只用于主要按钮、焦点、进度和关键状态 |
| 视觉稿 | `docs/images/ludora-workbench-ui-concept-v1.png` |

## 2. 本轮确认的产品规则

1. 冷启动先进入简化首页。
2. 首页核心只突出“描述游戏创意”和“开始制作”。
3. 项目页保持左侧项目、中间制作记录、右侧游戏预览。
4. 模型、Reasoning Effort、FPS 和图片能力放入高级设置。
5. 技术事件默认转换成普通用户能理解的制作记录，原始内容放入详细日志。
6. 页面以中性灰为主，减少主题色面积。
7. 所有 Tab、导航分类和切换选中态，禁止使用左侧彩色竖条。

## 3. 已完成开发

### 3.1 首页

- [x] 新增 `HomeWorkspace.tsx`。
- [x] 冷启动默认进入首页。
- [x] 增加大尺寸游戏创意输入框。
- [x] 增加 3 个创意示例。
- [x] 展示最近 3 个项目。
- [x] 增加可折叠高级设置。
- [x] 在开始制作前检查 Runtime、Codex 登录和图片能力。
- [x] 创建项目后自动打开项目并启动 Agent。
- [x] 创建与启动拆成两个明确步骤，直接使用创建接口返回的 `project.id`。
- [x] 增加默认项目名生成函数及 4 个测试。

### 3.2 游戏制作工作台

- [x] 项目栏支持桌面收起和展开。
- [x] “新建游戏”返回简化首页。
- [x] 高级创建入口移入顶栏更多菜单。
- [x] Pipeline 从 8 张阶段卡片改为紧凑横向进度条。
- [x] Composer 默认只展示输入框与开始/停止按钮。
- [x] 模型、Reasoning Effort、FPS 和图片能力移入高级设置。
- [x] EventStream 增加普通视图与详细日志切换。
- [x] 普通视图隐藏 Thread ID、事件方法名和工具原始内容。
- [x] Inspector 标题改为中文。
- [x] 深色、浅色主题调整为中性灰视觉系统。

### 3.3 Tab 与切换选中态

| 位置 | 当前选中表现 | 左侧彩色竖条 |
| --- | --- | --- |
| 左侧项目列表 | 中性背景、文字对比与状态圆点 | 已移除 |
| 设置分类 | 中性背景对比 | 已移除 |
| 提示词分类 | 背景与文字对比 | 已移除 |
| 预览 / 素材 / 文件 Tab | 底部细线 | 不使用 |
| FPS 选择 | 完整边框与底部提示 | 不使用 |
| 主题选择 | 完整边框 | 不使用 |

说明：错误提示、Runtime 状态、图片能力提示等信息块仍可以使用状态色边线，因为它们表达“状态或警告”，不属于 Tab 或切换选中态。

## 4. 主要变更文件

| 文件 | 变更内容 |
| --- | --- |
| `src/renderer/App.tsx` | 首页与项目工作台入口、创建后启动流程、菜单与状态组织 |
| `src/renderer/components/HomeWorkspace.tsx` | 新首页、创意输入、最近项目和高级设置 |
| `src/renderer/components/ProjectRail.tsx` | 项目栏简化与收起能力 |
| `src/renderer/components/Pipeline.tsx` | 紧凑制作进度 |
| `src/renderer/components/Composer.tsx` | 简化输入区与高级设置 |
| `src/renderer/components/EventStream.tsx` | 普通制作记录与详细日志 |
| `src/renderer/components/Inspector.tsx` | 中文标题与 Tab 展示 |
| `src/renderer/styles.css` | 中性灰视觉系统、首页布局、工作台布局和选中态规则 |
| `src/renderer/ui.ts` | 默认项目名生成等 UI 辅助逻辑 |
| `src/renderer/ui.test.ts` | 默认项目名相关测试 |

本轮没有新增依赖，没有修改 Electron Main、IPC、Agent、Prompt、Skill、媒体生成或安全业务逻辑。

## 5. 验证记录

### 5.1 自动化验证

执行 `npm run verify`，结果：

- [x] 19 个测试文件通过。
- [x] 128 个测试通过。
- [x] TypeScript 类型检查通过。
- [x] Production build 通过。

执行 UI smoke：

```bash
env -u ELECTRON_RUN_AS_NODE npm run smoke:ui
```

结果：

- [x] Electron 成功启动。
- [x] 工作台成功渲染。
- [x] 截图成功生成：`.ludora-smoke/workbench.png`。
- [x] 项目选中态经过实际界面检查，没有左侧彩色竖条。

执行 `git diff --check`，结果：

- [x] 没有空白字符或补丁格式错误。

### 5.2 环境说明

当前电脑环境存在：

```text
ELECTRON_RUN_AS_NODE=1
```

直接执行 `npm run smoke:ui` 会导致 Electron 的 `BrowserWindow` 导出异常。临时取消该环境变量后可以正常执行，这不是 Ludora 项目代码问题。

## 6. 待人工确认

以下内容建议由使用者在实际窗口中再体验一遍：

- [ ] 浅色主题下的文字与边界是否舒适。
- [ ] 小窗口下首页、项目栏和预览是否符合使用习惯。
- [ ] 键盘切换主要 Tab、打开高级设置和提交指令是否顺手。
- [ ] 设置分类、提示词分类逐项切换时的视觉反馈是否满意。
- [ ] “主题色只少量使用”的整体程度是否符合预期。

## 7. Git 状态

本轮代码与文档当前保存在本地工作区，尚未为本轮改造创建 Git 提交，也尚未推送到远程仓库。

提交前应再次执行：

```bash
git status --short
git diff --check
npm run verify
```

## 8. 后续记录规则

后续每次 UI 调整都在本文追加一条记录，至少包含：

1. 日期与需求原话。
2. 实际修改位置。
3. 修改前后差异。
4. 验证命令与结果。
5. 尚未验证的内容。
6. 是否已经提交和推送。

### 2026-09-04：取消选择项左侧彩色竖条

- 需求：所有 Tab 选择或切换选择时，不要出现左侧带颜色的竖条。
- 修改：移除项目项、桌面设置分类和桌面提示词分类选中态的左侧主题色指示。
- 统一规则：选择状态使用中性背景、文字对比、完整边框或底部细线表达。
- 验证：源码扫描、实际项目页截图、`git diff --check` 均通过。

### 2026-09-05：用品牌循环视频替换等待小游戏（已废弃，仅作历史记录）

> 状态：该方案随后被用户取消，MP4 文件和全部生产引用已经删除。以下内容仅保留为历史决策记录，不代表当前实现。

- 需求：取消 Chrome 恐龙跳跃方向；游戏尚未生成时不再展示占位小游戏，改为与 Ludora Logo 宣传图一致的约 15 秒循环动图视频。
- 旧任务处理：已停止仍在后台运行的“横版跳跃游戏”制作任务；保留已产生文件，没有删除项目。
- 品牌来源：复用 `docs/images/ludora-game-agent-poster.png`，不重新生成或改变品牌主体。
- 视频输出：`assets/ludora-wait-loop.mp4`，H.264、1280×720、30 FPS、451 帧、15.03 秒、无音频。
- 循环方式：首尾使用相同构图与运动参数，通过呼吸缩放、轻微漂移和亮度变化形成无缝循环。
- 回退图片：`assets/ludora-wait-poster.webp`；视频加载失败或用户启用减少动态效果时仍有可见画面。
- 页面行为：使用 `autoplay muted loop playsinline` 自动静音播放，显示“正在构建游戏世界”和 Ludora 品牌文案。
- 模板范围：以后新建的 Ludora 项目都会获得该等待视频；真正游戏完成后由 Agent 替换等待页。
- 当前预览：同步更新 `/Users/leco/Desktop/game/银铲铲`，并在 Ludora 实际窗口中确认画面加载成功。
- 打包：将 `assets/**/*` 加入 Electron 安装包文件清单。
- 稳定性：修复循环视频分段读取时预览服务器文件句柄未显式关闭的问题。
- 验证：视频完整解码 451 帧；当前项目使用 Ludora 已有 Vite 成功构建；`npm run verify` 通过，19 个测试文件、128 个测试、类型检查和生产构建全部成功。
- Git：本轮改动仍在本地，尚未提交或推送。

### 2026-09-05：取消等待视频，改为完全静态的宣传图

- 需求：不再使用视频；等待页只显示静态 Ludora 品牌宣传图，画面不播放、不缩放、不呼吸、不闪动。
- 保留图片：主项目使用 `assets/ludora-wait-poster.webp`，当前“银铲铲”项目使用 `public/assets/ludora-wait-poster.webp`。
- 页面修改：`<video>` 替换为单个原生 `<img>`；移除 `autoplay`、`loop`、视频源、15 秒标记和所有播放控制逻辑。
- 动效修改：移除等待状态点的呼吸动画、关键帧和减少动态效果分支；页面中不再存在 CSS 动画或过渡。
- 模板范围：以后新建的 Ludora 项目只复制静态 WebP，不再复制或引用 MP4。
- 当前项目：同步修改 `/Users/leco/Desktop/game/银铲铲` 的页面、脚本、样式和 `GAME_DESIGN.md`，并重新生成 `dist`。
- 动画评估：presentation=`2d`，generation=`not-needed`；等待画面按用户要求保持完全静止，游戏正式实现后的 60 FPS 规则不受影响。
- 已删除视频：`assets/ludora-wait-loop.mp4`、`/Users/leco/Desktop/game/银铲铲/public/assets/ludora-wait-loop.mp4`、`/Users/leco/Desktop/game/银铲铲/dist/assets/ludora-wait-loop.mp4`。
- 恢复说明：上述 MP4 此前未提交 Git，不能从 Git 直接恢复；保留的宣传图可用于日后重新生成。
- 自动验证：相关 11 个测试通过；`npm run verify` 通过，19 个测试文件、128 个测试、TypeScript 类型检查和生产构建全部成功；`git diff --check` 通过。
- 页面验证：静态图片成功加载为 1280×720；页面无 `<video>`、Canvas、CSS 动画或过渡，控制台无错误；图片区域间隔 3 秒的两次截图完全一致。
- Git：本轮改动仍在本地，尚未提交或推送。

### 2026-09-06：按游戏工作台参考统一全局 UI

> 本条是对 2026-09-04 开发手册的视觉与布局补充：工作台由原来的对话在中间，调整为**预览在中间、制作对话在右侧**。原有业务流程与安全边界不变。

#### 需求与实际完成

- 用户确认“开始吧”：参考游戏面板的深灰外框、灰蓝配色、切换方式、字体层级、卡片尺寸和游戏化图标。
- 顶层改为蓝灰环境底色 + 深色圆角应用外壳；顶栏统一品牌、首页/工作台切换、账户与设置入口。
- 左侧保留项目导航、折叠与新建入口；项目改用插画图标。选中项以完整蓝色背景或中性背景表达，**没有左侧彩色竖条**。
- 首页保留“一句话开始制作”表单、示例和最近项目；增加静态品牌图与规划/开发/检查角色说明。
- 制作页：中间为预览/素材/文件，预览下方增加最近素材卡片和八阶段进度；右侧为头像化对话与指令输入。
- 设置七个分类、提示词角色导航、新建项目、审批、文件与素材页统一字号、圆角、边框和控件样式。
- 预览按 16:9 放入可用空间，完整显示画面；未占用部分留深色边，不修改游戏本身的内容。
- 继续保留浅色主题；窄窗口切换上下排列，手机宽度改用抽屉导航。
- 原等待图继续完全静态；本轮没有添加视频，也没有修改已生成的游戏项目。

#### 当前实现尺寸与配色

以下为落地代码的 CSS 参数，属于参考风格的响应式适配，并非声称还原了参考图的原始设计源文件数值。

| 项目 | 参数 |
| --- | --- |
| 主背景 / 面板 / 浮层 | `#181B24` / `#222733` / `#2B303D` |
| 主要文字 / 辅助文字 | `#F2F4FA` / `#B0B8C8` |
| 主按钮渐变 | `#5664ED` → `#4654D9`，白字 |
| 外壳圆角 / 桌面外边距 | 26px / 24px；小窗口逐步收紧 |
| 顶栏 / 项目栏 / 右侧对话 | 64px / 220px / 340px；中等窗口 196px / 306px |
| 标题 / 正文 / 辅助说明 | 首页 28px，弹窗 20–22px，正文 13–14px，常规辅助文字 12px |
| 卡片 / 按钮圆角 | 卡片 12–16px，按钮约 8–10px |
| 响应式断点 | 1279、1099、799、599px |

#### 代码位置与可编辑入口

- `src/renderer/workbench.css`：新增的统一视觉层，加载在原 `styles.css` 之后；颜色、间距、字号、卡片和响应式优先在这里调整。
- `src/renderer/App.tsx`：总布局与顶栏；“更多”菜单支持选中操作、点击外部和 Escape 自动收起。
- `src/renderer/components/HomeWorkspace.tsx`、`ProjectRail.tsx`：首页与导航。
- `src/renderer/components/Inspector.tsx`：预览尺寸、最近素材、Tab 键盘操作；左右方向键、Home、End 切换。
- `src/renderer/components/EventStream.tsx`、`Pipeline.tsx`：角色头像与阶段标记。
- `src/renderer/components/GameGlyph.tsx`：图集裁切与项目图标分配。
- `src/renderer/components/SettingsExtensions.tsx`、`SettingsModal.tsx`：提示词头像与设置文案；不改提示词内容或持久化接口。
- 配套图集来源及提示词：[图集说明](../docs/images/ludora-workbench-atlas.md)。首页宣传图复用现有 `assets/ludora-wait-poster.webp`。

haokan-skill 用于统一参考图的美术方向、字体/留白和渲染验收；没有借此更换技术栈或引入 UI 框架。新增图标共一张本地图集，功能性小图标继续使用原有 Lucide。

#### 验证结果

- `npm run verify`：类型检查、19 个测试文件 / 128 个测试、Renderer 与 Main 生产构建通过。
- 最后视觉微调后再次执行 `env -u ELECTRON_RUN_AS_NODE npm run smoke:reference`：27 张真实 Electron Renderer 截图及交互断言通过，无 Renderer 控制台错误。
- 覆盖首页、预览/素材/文件、对话高级设置、七个设置分类、新建项目、审批、浅色主题、折叠导航及抽屉导航。
- 覆盖 1510×940、1160×820、760×620、390×844 CSS 像素；检查整页无横向溢出、桌面素材栏可见、头像裁切与阶段网格正确。
- 验证首页“创建后开始”、工作台继续/停止、文件读取、日志展开、Tab 方向键、弹窗 Tab 焦点限制和 Escape 关闭、菜单自动收起。
- 检查主按钮白字在渐变较浅端对比度约 4.73:1，深色辅助字在悬停底色上约 4.74:1；同步加深浅色主题辅助文字。该检查不等于完整第三方无障碍认证。
- `git diff --check` 通过。

截图与报告位于 `.ludora-smoke/reference-after/`，例如 `home-desktop.png`、`workspace-desktop.png`、`settings-提示词.png`、`mobile-project-navigation.png`、`report.json`。该目录不提交 Git，且项目 `clean` 命令会清除它，可用上述命令重建。

验收脚本使用临时用户目录和内存中的项目、账户、模型、事件数据；截图标注了 UI fixture。不会读取真实凭据、创建真实游戏或调用付费生成服务。该验收证明 Renderer 布局和接口调用正常，**不代表本轮重新验证了真实 Codex 登录、媒体 API 付费生成、MCP 连接或完整游戏交付**。

#### Git 与保留项

- 本轮仅保存在本地，未提交、未推送。
- 保留工作区中原本未跟踪的 `docs/images/noobi-game-agent-poster.png` 和 `src/renderer/assets/noobi-app-icon.png`，未修改或删除。
- 未修改 `src/main/`、`src/shared/`、协议生成文件、项目 Skill、Prompt 模板或实际游戏目录。

### 2026-09-06：扩大预览、流程并入对话、仅保留深色

> 以本条为准：覆盖上一轮的“预览底部素材区与流程”“外围紫色背景”“支持浅色主题”设计。

- 移除 `Inspector.tsx` 中预览下方的“游戏素材”快捷区，只移除展示，不删除素材文件；顶部“素材”Tab 和素材管理功能保留。
- 从中间画布移走 `Pipeline`，放到 `EventStream.tsx` 的对话记录中，展示为“制作流程”卡片，八个阶段以四列两行排列，随当前项目阶段与状态更新。
- 中间区域仅保留检查器；预览 iframe 填满剩余空间，不再强制缩进 16:9 小框。游戏内部自身的响应式布局与画面比例由游戏代码决定，本轮没有修改游戏文件。
- 删除外围紫色渐变、外边距和紫色阴影，应用直接铺满窗口，保留内部深灰卡片和蓝色按钮。
- 删除顶栏主题切换与设置中的“外观”分类；设置现在保留账户、媒体 API、项目默认值、Skills、MCP Servers、提示词六类。
- 移除浅色 CSS 和跟随系统浅色规则；HTML 从首帧就是深色，`App.tsx` 对旧宿主返回的浅色偏好也统一使用深色。
- `AppSettings.theme` 限定为 `dark`；`ProjectStore` 仍兼容读取旧 `light` 值并原子迁移到 `dark`，保留项目清单、目录、模型与推理设置。
- Electron `nativeTheme.themeSource` 固定为 `dark`，同步原生标题栏、菜单及控件；只调整主进程的外观启动设置与偏好迁移，不修改 Agent、Prompt、Skill 或素材生产逻辑。
- 新增旧浅色偏好迁移测试，确认除了主题之外的项目和设置不变，且保存、重载后仍为深色。
- 验证：`npm run verify` 通过，19 个测试文件 / 129 个测试、类型检查、Renderer 与 Main 构建成功。
- UI 验证：`env -u ELECTRON_RUN_AS_NODE npm run smoke:reference` 通过，25 张页面截图、四种窗口尺寸和交互检查；特意模拟“系统浅色 + 旧浅色偏好”，所有页面仍为深色。
- 1510×940 验收窗口中，游戏 iframe 为约 888×692 CSS 像素；断言确认没有底部素材区，流程只在对话区，应用铺满窗口且不存在外围渐变。
- 截图与报告仍输出至 `.ludora-smoke/reference-after/`，均为隔离演示数据，不调用真实 AI 任务。
- 本轮改动保存在本地，未提交、未推送；原有未跟踪旧品牌图片继续保留。
- 实际应用复查：确认无运行中的制作任务后重启 Ludora，真实横版跳跃项目的预览已扩大，流程位于右侧记录，原生标题栏为深色；本机旧浅色偏好已迁移为 `dark`，项目数量仍为 2，未启动新的 AI 制作任务。
- 最终 `git diff --check` 通过；开发服务已重新运行。

### 2026-09-06：修复高级设置中的帧率选项重叠

- 反馈：展开对话输入区的高级设置后，30 / 60 / 120 FPS 选项挤在左侧并重叠。
- 原因：紧凑帧率控件沿用了 `auto minmax(190px, 360px)` 的横向两列 fieldset 布局，无法适配现在的窄对话面板；旧的整页溢出检查没有覆盖选项内部文字。
- 修改 `src/renderer/styles.css`：紧凑 fieldset 改为纵向结构，标题在上；选项区使用完整宽度的三等分网格，每张卡片内数字与 FPS 上下排列，并删除旧的窗口宽度补丁。
- 修改 `src/renderer/workbench.css`：增加高级设置分组间距，允许素材要求说明换行；滚动限制在输入与设置内容区，底部继续制作/停止按钮保留在可见区域。
- 修改 `scripts/ui-reference-smoke.cjs`：增加选项宽度、文字边界、选项间距、分组溢出及底部按钮可见性断言；先用旧版本复现断言失败，修复后通过。
- 验证 1510、1160、760、390px 四种窗口宽度下的高级设置，三张卡片分别约 84、73、211、91px 宽，均无重叠或文字溢出；鼠标选择 120 FPS、方向键回到 60 FPS 正常。
- `env -u ELECTRON_RUN_AS_NODE npm run smoke:reference` 通过：Renderer 构建、28 张 UI 截图和交互检查成功；`npm test` 通过：19 个测试文件、129 个测试；`git diff --check` 通过。
- 截图：`.ludora-smoke/reference-after/composer-advanced.png`、`composer-advanced-1160.png` 等。
- 本轮仅修改样式、验收脚本和开发记录，没有修改帧率数据、AI 任务参数或主进程；没有重启应用，也没有停止当前制作任务。改动未提交、未推送。

### 2026-09-06：将制作流程精简为单行

> 以本条为准：替代此前对话区的“制作流程”标题、进度条和八阶段图标网格。

- 根据“只展示当前流程和总流程步数”的反馈，对话记录中的流程改为约 42px 高的单行：左侧显示当前阶段，右侧显示当前步数 / 总步数，例如 `代码实现 · 6 / 8`。
- `src/renderer/components/Pipeline.tsx` 仅保留阶段名称和步数，继续使用原有阶段计算与项目状态；保留屏幕阅读器可读取的阶段、进度和状态信息。
- `src/renderer/workbench.css` 删除阶段网格及其响应式覆盖，统一单行间距、字号和步数样式；不增加左侧彩色竖条。
- `scripts/ui-reference-smoke.cjs` 增加单行高度、阶段名称、步数及无障碍属性检查，确认旧标题、图标网格和进度条已移除；覆盖新项目 `需求拆解 · 1 / 8`、制作中及完成状态 `完成交付 · 8 / 8`。
- 验证：`npm run typecheck` 通过；`env -u ELECTRON_RUN_AS_NODE npm run smoke:reference` 通过，Renderer 构建、29 张 UI 截图及交互断言成功；`git diff --check` 通过。本轮没有重新运行完整单元测试套件。
- 截图：`.ludora-smoke/reference-after/workspace-desktop.png`、`pipeline-completed.png`；使用隔离演示数据，不调用真实 AI 制作任务。
- 开发服务热更新生效，没有重启应用或停止当前制作任务；不修改后台流程、Prompt、Skill 或游戏文件。改动仅保存在本地，未提交、未推送。

### 2026-09-06：制作流程支持点击展开与收起

> 在上一条单行设计上增加按需展开：默认仍为精简行，完整流程不再直接占用对话空间。

- 点击整行当前阶段 / 步数，可展开“制作流程”标题、项目状态、进度条与八个图标阶段卡片；保持四列两行，区分已完成、当前阶段、待进行。再次点击同一行收起。
- 单行继续保持约 42px 高，增加小箭头提示展开方向；沿用深灰与蓝色，不增加左侧彩色竖条。
- `Pipeline.tsx` 使用原生 `details / summary`，支持鼠标、Enter 与空格键；保留可访问的阶段、步数和项目状态，展开后进度条与当前阶段也有对应语义。
- 展开时将流程滚动至对话区可见范围；同一项目的阶段更新不重置展开状态。`EventStream.tsx` 按项目 ID 区分组件，切换项目后默认收起，不把上一个项目的展开状态带过去。
- `workbench.css` 增加展开内容、四列卡片、悬停和键盘焦点样式；`ui-reference-smoke.cjs` 覆盖默认收起、重复点击、键盘操作、项目切换、阶段实时更新和完成状态，确认折叠操作不会触发制作或停止接口。
- 验证：`npm run typecheck` 通过；`npm test` 通过，19 个测试文件 / 129 个测试；`env -u ELECTRON_RUN_AS_NODE npm run smoke:reference` 通过，Renderer 构建、34 张截图与交互断言成功。检查 1510、1160、760、390px 四种窗口宽度，无阶段卡片文字溢出或页面横向溢出；`git diff --check` 通过。
- 截图：`.ludora-smoke/reference-after/pipeline-expanded.png`、`pipeline-expanded-1160.png`、`pipeline-completed-expanded.png` 等；验收使用隔离演示数据，不调用真实 AI 服务。
- 本轮仅修改流程组件、样式、对话区组件标识、验收脚本和本记录；没有重启应用、停止当前任务或修改后台制作流程。改动保存在本地，未提交、未推送。
